{"id":1005972410,"lang":"cpp","lang_name":"C++","time":"2 months","timestamp":1690531174,"status":10,"status_display":"Accepted","runtime":"7 ms","url":"/submissions/detail/1005972410/","is_pending":"Not Pending","title":"Relative Ranks","memory":"10.8 MB","code":"class Solution {\npublic:\n    vector<string> findRelativeRanks(vector<int>& score) {\n        vector<string> res;\n        res.resize(score.size());\n        unordered_map<int, int> m;\n\n        for (auto i = 0; i < score.size(); i++)\n            m[score[i]] = i;\n\n        sort(score.begin(), score.end());\n        for (auto i = 0; i < score.size(); i++) {\n            if (i == score.size() - 1)\n                res[m[score[i]]] = \"Gold Medal\";\n            else if (i == score.size() - 2)\n                res[m[score[i]]] = \"Silver Medal\";\n            else if (i == score.size() - 3)\n                res[m[score[i]]] = \"Bronze Medal\";\n            else\n                res[m[score[i]]] = to_string(score.size() - i);\n        }\n\n        return res;\n    }\n};","compare_result":"11111111111111111","title_slug":"relative-ranks","has_notes":false,"flag_type":1}